package com.imsweb.datagenerator;

public interface DataGeneratorRule {

    /**
     * Returns the rule's unique identifier.
     */
    String getId();

    /**
     * Returns the rule's name.
     */
    String getName();
}
